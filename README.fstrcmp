NAME

fstrcmp — calculate the similarity of two strings

SYNOPSIS

fstrcmp(text string1, text string2, double limit)
fstrcmp(text string1, text string2)

Inputs

	string1, string2 — strings to compare
	limit — minimum similarity of two strings
